namespace SaaS.Infrastructure.IntegrationTests;

// Integration tests require a running Postgres instance (via Testcontainers).
// Placeholder — real tests will be added in subsequent iterations.
public sealed class PlaceholderTests
{
    [Fact]
    public void Placeholder_passes() => Assert.True(true);
}
